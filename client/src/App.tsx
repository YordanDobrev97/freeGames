import { Component, ReactNode } from 'react'

type State = { 
}

export default class App extends Component<any, State> {
  constructor() {
    super({})
    this.state = {
    }
  }

  render(): ReactNode {
    return (
      <>
      </>
    )
  }
}
